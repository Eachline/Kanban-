import styled from 'styled-components';

export const StyledIcon = styled.div`
  display: inline-flex;
  align-items: center;
  text-align: center;

  svg {
    display: inline-flex;
    width: 100%;
    height: max-content;
  }
`;
